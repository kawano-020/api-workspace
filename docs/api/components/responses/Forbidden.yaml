description: |
  **Forbidden**
  リクエストを実行する権限がない等
content:
  application/json:
    schema:
      $ref: "#/components/schemas/Error"
    example:
      code: 403
      message: Insufficient permissions to a resource.
